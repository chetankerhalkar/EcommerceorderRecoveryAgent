# Minimal requirements for AICK Studio Abandoned Cart Recovery Agent
# Use this if you encounter dependency conflicts with requirements.txt

# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# LangGraph and LangChain - Let pip resolve compatible versions
langgraph
langchain
langchain-openai
langchain-community

# HTTP and API clients
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.9.0

# Email
sendgrid>=6.10.0

# OpenAI
openai>=1.0.0

# Utilities
python-multipart>=0.0.6

# Optional: Remove if not needed
# sqlalchemy>=2.0.0
# apscheduler>=3.10.0
